"use client";

import { ParamProps } from '@/types/appNode';
import React from 'react'

export default function SelectParam({param}: ParamProps) {
  return (
    <div className="flex flex-col gap-1 w-full">Select</div>
  );
}
